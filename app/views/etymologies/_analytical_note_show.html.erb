<% edit_path = etymology_analytical_note_edit_url(:id => e.id) %>
<%  if e.analytical_note == nil or e.analytical_note == '' 
  t_analytical = 'Click to modify'
else
  t_analytical = e.analytical_note
end %>
<%=  "#{link_to_remote("#{t_analytical}", :url => edit_path, :update => "#{e.id}_anotediv", :method => :get ) }" %>