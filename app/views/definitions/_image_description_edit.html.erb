<%= wide_tagged_tinymce_script %>
<% form_for :definition, :url => { :action => "update_image_description" } do |f| %>
<p>
<%= f.hidden_field :id %>
<%= f.text_area :image_description, :class => 'mceEditor' %>
<%= submit_to_remote 'update_button','update Image Description', :url => { :action => "update_image_description" }, :update => "#{d.id}_imagedescdiv", :before => "tinyMCE.triggerSave();" %>
<%#= link_to_remote 'Cancel', :url => {:action => "image_description_show", :params => {'id' => params['id'] }}, :update => "#{d.id}_imagedescdiv" %>
<%= link_to_remote 'Cancel', {:url => image_description_show_path(params['id']), :method => 'get', :update => "#{d.id}_imagedescdiv" } %>
</p>
<% end %>