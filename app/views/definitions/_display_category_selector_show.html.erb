<% 
@data = Category.find(data_id)
@definition = Definition.find(id)
@model_name = 'definition'
@function_name = function_name
@id = id
@update_id = update_id
%>

<% if @function_name == 'thematic_classification_type' %>
<%=   @definition.thematic_classification_type.title %>
<%
      edit_path = thematic_classification_edit_url(:id => @definition.id)
      t_definition = 'Click to modify'  %>
<%#=  "#{link_to_remote("#{t_definition}", :url => edit_path, :update => "definition[thematic_classification_type#{@definition.id}]_selector", :method => :get, :html => {:class => 'definitions_show'} ) }" %>

<%#=  "#{link_to_remote("#{t_definition}", :url => edit_path, :update => "thematic_classification_type#{@definition.id}", :method => :get, :html => {:class => 'definitions_show'} ) }" %>
<%=  "#{link_to_remote(image_tag('pencil.png',:border => 0), :url => edit_path, :update => "thematic_classification_type#{@definition.id}", :method => :get, :html => {:class => 'definitions_show'} ) }" %>

<% else
    if @function_name == 'grammatical_function_type' %>

<%  end %>
<% end %>