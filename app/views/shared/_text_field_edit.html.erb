<%#= wide_tagged_tinymce_script %>
<% show_action = text_show_action(params[:action]) 
   update_action = text_update_action(params[:action])
   controller = params[:controller] 
   field = dynamic_text_field(params[:action])
%>
<% form_for controller.singularize, :url => { :action => "update_analytical_note" } do |f| %>
<p>
<%= f.hidden_field :id %>
<%#= f.text_area field, :class => 'mceEditor' %>
<%= f.text_field field %>
<%= submit_to_remote 'update_button','Update', :url => { :controller => controller, :action => update_action}, :update => "#{t.id}#{divsuffix}" %>
<%= link_to_remote 'Cancel', :url => {:controller => controller, :action => show_action, :params => {'id' => params['id'] }}, :method => :get, :update => "#{t.id}#{divsuffix}"  %>
</p>
<% end %>